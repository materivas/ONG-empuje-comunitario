spring.application.name=SistemasDistribuidosTP

spring.datasource.url=jdbc:mysql://localhost:3306/${MYSQL_DB_NAME}
spring.datasource.username=${MYSQL_DB_USERNAME}
spring.datasource.password=${MYSQL_DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.security.jwt.user.generator=AUTH-SPRING-BACKEND
spring.security.jwt.private.key=${JWT_PRIVATE_KEY}